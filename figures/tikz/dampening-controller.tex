\documentclass[tikz, 14pt, border=10pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows,positioning,calc}

\begin{document}

\tikzset{
block/.style = {draw, fill=white, rectangle, minimum height=3em, minimum width=6em},
tmp/.style  = {coordinate}, 
sum/.style= {draw, fill=white, circle, inner sep=0pt},
joint/.style= {draw, fill=black, circle, inner sep=0pt, minimum size=0.1cm},
input/.style = {coordinate},
output/.style= {coordinate},
pinstyle/.style = {pin edge={to-,thin,black}
}
}

\begin{tikzpicture}[auto, node distance=9em,>=latex']
    \node [input, name=rinput] (rinput) {};
    \node [sum, right of=rinput, node distance=3.5cm] (sum1) {$-$};
    \node [block, right of=sum1, align=center] (damp-controller) {Dampening\\ Controller};
    \node [block, right of=damp-controller, align=center] (cmg-controller) {CMGs\\ \footnotesize{incl. Controller}};
    \node [block, right of=cmg-controller] (pendulum) {Pendulum};
    \node [block, below of=pendulum, node distance=1.5cm] (sensor) {Sensor};
    \node [joint, right of=pendulum, node distance=1.5cm] (joint1) {};
    \node [output, right of=joint1, node distance=1.5cm] (output) {};
    \node [tmp, below of=damp-controller, node distance=1.5cm] (tmp1) {};

    \draw [->] (rinput) -- node{$P_W = \theta_i, \dot{\theta}_i = 0$} (sum1);
    \draw [->] (sum1) -- node{$P_E$} (damp-controller);
    \draw [->] (damp-controller) -- node{$\tau_W$} (cmg-controller);
    \draw [->] (cmg-controller) -- node{$\tau_Y$} (pendulum);
    \draw [->] (pendulum) -- (joint1) -- node{$P_Y$} (output);
    \draw [->] (joint1) |- (sensor) -- node{$P_M$} (tmp1) -| (sum1);

\end{tikzpicture}

\end{document}
